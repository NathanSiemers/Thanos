% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thanos_module.R
\name{thanosServer}
\alias{thanosServer}
\title{Thanos cross-filter module server}
\usage{
thanosServer(
  id,
  backend,
  default_selected = NULL,
  bins = 50,
  debounce_ms = 300,
  debounce_checkbox_ms = 300,
  plot_height = "150px",
  max_checkbox_levels = 30,
  mode = c("auto", "vector", "aggregate"),
  aggregate_threshold = 2e+06,
  remember_removed = FALSE,
  removal_note = TRUE,
  max_discrete_numeric = 12,
  plot_engine = c("base", "ggplot")
)
}
\arguments{
\item{id}{Module id; must match the \code{id} given to \code{\link[=thanosUI]{thanosUI()}}.}

\item{backend}{A backend object, e.g. from \code{\link[=backend_memory]{backend_memory()}},
\code{\link[=backend_sqlite]{backend_sqlite()}}, or \code{\link[=backend_duckdb]{backend_duckdb()}}.}

\item{default_selected}{Character vector of columns pre-selected for
filtering at startup (unknown names are ignored).}

\item{bins}{Number of histogram bins for continuous numeric columns.}

\item{debounce_ms}{Debounce interval (ms) for slider inputs.}

\item{debounce_checkbox_ms}{Debounce interval (ms) for checkbox
groups and selectize filters.}

\item{plot_height}{CSS height of each histogram.}

\item{max_checkbox_levels}{Categorical columns with at most this many
levels get checkboxes; above it, a selectize input.}

\item{mode}{\code{"auto"} (default), \code{"vector"}, or \code{"aggregate"}.
Vector mode fetches whole columns once and filters in R; aggregate
mode never fetches columns and asks the backend for binned counts
via SQL. \code{"auto"} picks aggregate when the backend supports it and
the data exceeds \code{aggregate_threshold} rows.}

\item{aggregate_threshold}{Row count above which \code{"auto"} switches to
aggregate mode.}

\item{remember_removed}{If \code{TRUE}, a removed column's filter settings
are kept and restored when it is re-added; if \code{FALSE} (default)
removing a column clears its filter completely.}

\item{removal_note}{Show the one-line in-page note explaining what
removing a column does.}

\item{max_discrete_numeric}{Numeric columns with at most this many
distinct values are treated as discrete (checkboxes with
membership semantics) instead of getting a range slider.}

\item{plot_engine}{\code{"base"} (default; identical visual at a fraction
of the rendering cost) or \code{"ggplot"}.}
}
\value{
A list of accessors for the parent app:
\describe{
\item{\code{mask()}}{reactive: \code{logical(n_rows)}, \code{TRUE} = row passes
all filters}
\item{\code{rows()}}{reactive: integer row IDs passing all filters}
\item{\code{n_selected()}}{reactive: number of rows passing}
\item{\code{selected_vars()}}{reactive: currently selected columns}
\item{\code{filters()}}{reactive: named list of current filter values
in raw units (save/restore, bookmarking)}
\item{\code{streams(v, split_range = FALSE, drop_na = FALSE)}}{function:
leave-one-out row streams for column \code{v} -- the rows passing
every \emph{other} filter, partitioned by what \code{v}'s own filter did
to them. Returns sorted row-ID vectors: \code{selected} (also passes
\code{v}'s filter; identical to \code{rows()} when \code{v} is selected) and
\code{excluded}, or with \code{split_range = TRUE} on a range filter
\code{selected}/\code{below}/\code{above}/\code{na} (rejects split by side; a
slider handle at its endpoint leaves that side empty; excluded
NAs, having no side, land in \code{na}). NA rows follow the
include-NA checkbox like the mask; \code{drop_na = TRUE} removes
them from every stream. Fetch data as
\code{backend$get_column(v)[ids]}.}
\item{\code{add_vars(cols)}}{function: ensure these columns appear in
the filter selection (additive, idempotent)}
}
}
\description{
Drives the cross-filter module against any backend implementing the
Thanos backend contract (see \code{\link[=backend_memory]{backend_memory()}}, \code{\link[=backend_dbi]{backend_dbi()}}).
Each selected column gets an auto-chosen filter widget (slider,
checkboxes, or selectize) and a live histogram of the rows passing
all \emph{other} filters, with this variable's own selection overlaid.
}
\examples{
\donttest{
library(shiny)
backend <- backend_memory(as.data.frame(mtcars))
ui <- fluidPage(
    thanosUI("thanos"),
    verbatimTextOutput("n")
)
server <- function(input, output, session) {
    th <- thanosServer("thanos", backend,
                       default_selected = c("mpg", "cyl"))
    output$n <- renderText(th$n_selected())
}
if (interactive()) shinyApp(ui, server)
}
}
