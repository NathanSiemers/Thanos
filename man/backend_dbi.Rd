% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thanos_backend_sqlite.R
\name{backend_dbi}
\alias{backend_dbi}
\title{DBI backend over the tall/skinny Thanos schema}
\usage{
backend_dbi(
  con,
  table = "long_data",
  registry = "column_registry",
  cache = TRUE,
  cache_max_entries = 256
)
}
\arguments{
\item{con}{A \code{DBI} connection to a database holding the tall/skinny
schema.}

\item{table}{Name of the tall/skinny data table.}

\item{registry}{Name of the column-registry metadata table.}

\item{cache}{Cache fetched columns and memoise aggregate query
results (assumes the database is immutable while open).}

\item{cache_max_entries}{Maximum number of memoised aggregate query
results (oldest evicted first).}
}
\value{
A backend implementing the Thanos backend contract, with the
aggregate-mode extensions (\code{supports_binned}, \code{get_binned},
\code{get_binned_pair}, \code{get_count}, \code{get_row_mask}), caching accessors
(\code{clear_cache}, \code{cache_stats}), and \code{disconnect()}.
}
\description{
Implements the Thanos backend contract for any DBI connection
holding the tall/skinny schema \code{long_data(row_id, column_name, value_num, value_txt)} plus a \code{column_registry} metadata table (see
the build scripts under \verb{db/} in the Thanos repository). Beyond the
column-store contract it advertises the aggregate capability
(\code{supports_binned}): histogram counts, row counts, and row masks are
computed by SQL \verb{GROUP BY} queries, so tens of millions of rows can
be filtered without a column vector ever entering R.
}
\details{
Fetched columns and aggregate query results are cached by default;
the cache assumes the database is immutable while the backend is
open. Call \verb{$clear_cache()} after a data change and
\verb{$cache_stats()} for hits/misses/bytes.
}
\examples{
\dontrun{
con <- DBI::dbConnect(RSQLite::SQLite(), "db/data/flights.sqlite")
backend <- backend_dbi(con)
backend$get_columns()
backend$disconnect()
}
}
