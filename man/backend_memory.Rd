% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thanos_backend.R
\name{backend_memory}
\alias{backend_memory}
\title{In-memory backend wrapping a data frame}
\usage{
backend_memory(df)
}
\arguments{
\item{df}{A data frame (or anything \code{as.data.frame()} accepts).}
}
\value{
A backend: a named list of functions (\code{get_columns},
\code{n_rows}, \code{get_column}, \code{get_column_info}) implementing the Thanos
backend contract, suitable for \code{\link[=thanosServer]{thanosServer()}}.
}
\description{
Wraps a data frame in the Thanos backend contract: the module treats
it as a column store and fetches each column at most once while it
stays selected. Factors and logicals are coerced to character,
Date/POSIXct to numeric (epoch values), integers to double;
non-atomic (e.g. list) columns are dropped.
}
\examples{
backend <- backend_memory(mtcars)
backend$n_rows()
backend$get_columns()
backend$get_column_info("mpg")$is_numeric
}
