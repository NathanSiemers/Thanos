% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thanos_module.R
\name{thanosUI}
\alias{thanosUI}
\title{Thanos cross-filter module UI}
\usage{
thanosUI(id, width = "100\%")
}
\arguments{
\item{id}{Module id; must match the \code{id} given to \code{\link[=thanosServer]{thanosServer()}}.}

\item{width}{CSS width of the column picker (default \code{"100\%"}).}
}
\value{
A \code{\link[shiny:reexports]{shiny::tagList()}} of UI elements to place in your layout.
}
\description{
Places the module's user interface: a column picker plus an anchor
\code{div} into which per-variable filter panels (widget + live
cross-filter histogram) are inserted and removed individually as the
user selects columns. Pair with \code{\link[=thanosServer]{thanosServer()}} using the same \code{id}.
}
\examples{
\donttest{
library(shiny)
backend <- backend_memory(mtcars)
ui <- fluidPage(thanosUI("thanos"))
server <- function(input, output, session) {
    th <- thanosServer("thanos", backend)
}
if (interactive()) shinyApp(ui, server)
}
}
