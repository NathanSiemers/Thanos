% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thanos_backend_sqlite.R
\name{backend_sqlite}
\alias{backend_sqlite}
\title{SQLite backend over the tall/skinny Thanos schema}
\usage{
backend_sqlite(
  db_path,
  table = "long_data",
  registry = "column_registry",
  cache = TRUE,
  cache_max_entries = 256,
  read_only = TRUE
)
}
\arguments{
\item{db_path}{Path to the SQLite database file (tall/skinny schema;
see the build scripts under \verb{db/} in the Thanos repository).}

\item{table}{Name of the tall/skinny data table.}

\item{registry}{Name of the column-registry metadata table.}

\item{cache}{Cache fetched columns and memoise aggregate query
results (assumes the database is immutable while open).}

\item{cache_max_entries}{Maximum number of memoised aggregate query
results (oldest evicted first).}

\item{read_only}{Open the database read-only (default \code{TRUE}).}
}
\value{
A backend as described in \code{\link[=backend_dbi]{backend_dbi()}}.
}
\description{
Thin wrapper around \code{\link[=backend_dbi]{backend_dbi()}} that opens a SQLite database
file, read-only by default so any number of processes can share it.
}
\examples{
\dontrun{
backend <- backend_sqlite("db/data/flights.sqlite")
backend$n_rows()
backend$disconnect()
}
}
